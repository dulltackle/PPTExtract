# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

- 长期产品由 Python/FastAPI API、React 策展工作台、持久 worker、PostgreSQL 权威状态库和 S3 兼容对象存储组成。
- 转换链路由项目从 OOXML 建立保守的单页投影，逐页调用并归一化 anydoc；精确锁定的 LibreOffice headless 与 Poppler 生成标准页渲染结果。
- 当前交互证据是单个原生 HTML/CSS/JavaScript 静态原型；它无后端和持久化，不是可复用的产品实现。

## Users

主要用户是熟悉材料的内部策展人员。他们会在桌面电脑上连续处理大量页，愿意学习并高频使用键盘快捷键。

产品还服务于创建文档和上传版本的摄取操作者、观察流程的策展负责人、明确确认发布的发布人员、整包验证并切换知识库的下游消费者，以及运维与依赖维护人员。初版只要求鉴权层提供稳定 `actor_id`；多人权限模型与具体 OAuth/SSO 方案尚不在范围内。

## Product Purpose

把持续增长、领域不限且含大量图表页的 `.pptx` 语料逐页转换、渲染并呈现给人，由人核对来源、处置视觉对象、补充标注并作出审核结论，再向下游 RAG 发布只含 approved、未软删页的完整权威产物。成功意味着人工判断可靠、身份与历史可追溯、未变化内容不重复劳动、下游永远只看到经完整验证后原子切换的一致产物，并为后续流程改进保留真实运行事实。

## Positioning

系统不把自动转换或模型当作质量来源：项目自己恢复可追溯的页边界和来源，人工策展是唯一质量闸门；跨版本稳定身份和指纹继承避免重复审核，冻结后的完整快照与原子指针切换则防止下游收到半新半旧的结果。

## Operating Context

- 完整日常路径是：创建文档或显式向既有文档上传新版本，轮询持久任务，必要时人工确认页对应，逐页策展，创建并确认发布候选，再由下游验证整包、导入隔离 generation 并整体切换。
- 首版审核界面以 1440px 桌面宽度和键盘流为主，保证 1280px 可用；移动端不在本次方案范围内。
- 策展人员对照标准页渲染结果，先审核 AnyDoc 提取文字和图片源文件；只有来源不足时才画框补充截图，再批准或排除页。
- 真实 `.pptx` 与其派生渲染仅限本地验证，受 Git 忽略规则保护；公开原型和 Issue 只使用脱敏仿制内容。
- 同一文档会反复上传；未变化页继承审核，变化页需要复核，歧义页对应需要人工确认。
- 发布是人的明确动作。pending 页不阻止发布，但不进入产物；构建、校验或对象写入失败时，当前产物继续可用。

## Capabilities and Constraints

- 初版只接收 OOXML `.pptx`。上传要求幂等键，只在源对象已完整持久化且数据库可靠接受后返回；同一文档最多一个非终态摄取任务，短暂错误有限重试，失败、作废与回滚都不篡改旧历史。
- 文档与页拥有跨版本稳定身份，版本与页版本是不可变记录；文件名、字节哈希、页码和页指纹都不承担身份。对应存在歧义时，新版本不能成为当前版本，旧当前版本继续服务。
- 系统在转换前登记源页清单、原始顺序、源引用和隐藏标记；隐藏页默认不转换、不渲染、不进入策展，只有人工启用后才走普通流程。
- 页指纹未变且对应确定时继承审核结论、标注和原审核信息；页变化后沿用页身份但回到 pending，旧内容只作带来源的待确认预填。
- AnyDoc 文字与全部图片源文件必须先由人确认；保留的源图片需要 `summary`，忽略的源图片需要原因。
- 画框截图是来源审核后的可选动作，不自动生成候选框；零截图是合法且可批准的页面状态。
- 人可以调整、合并、删除手动画出的视觉对象并改变语义顺序。正式视觉资产总是从标准页渲染结果中按人工确认范围裁出的无损 PNG，不单独交付 OOXML 原始媒体或整页图。
- 初版不包含 VLM 生成入口、草稿存储或相关任务与审核状态；若未来引入，仍只能由人显式触发并逐项确认。
- 批量操作只允许对所选 `pending` 页应用统一原因的排除；不允许批量批准、确认来源或套用标注内容。
- 标注辅助只提供字段结构、写作提示和示例，不提供包含事实内容的跨页模板。
- approved 或 excluded 页必须先重新打开为 pending 才能编辑。
- 当前一页对应一个 Chunk。Chunk 正文与有序 `parts` 由页标题、标注、来源正文、表格、图片 alt text 和演讲者备注确定性合成；页内不再切、不截断。
- 产物只含当前业务状态中 approved、未软删的页，并以包含 `manifest.json`、`chunks.jsonl` 与内容寻址 PNG 的不可变 ZIP 交付；不提供增量、墓碑或独立资产 API。
- 发布候选绑定一致业务状态；确认时冻结确切输入，异步构建与完整校验成功后才原子切换当前产物指针。下游通过 ETag 发现新产物，可流式或 Range 下载，完整验证后整体替换。
- PostgreSQL 是身份、状态、不可变历史、审计、幂等事实、持久任务、发布冻结清单和当前指针的唯一权威来源；二进制对象通过 SHA-256 内容寻址存储。持久任务按至少一次交付设计，各阶段必须幂等。
- 备份与恢复目标是 `RPO ≤ 5 分钟`、`RTO ≤ 4 小时`；恢复后在重开写入和发布前必须完成全量对象引用审计。
- 系统记录每页及各处理阶段耗时、待处理数量、最长等待时间、源图片与视觉对象数量以及动作计数，但不据此设置告警阈值或承诺产能、工时、SLA 或自动扩展策略。

## Evidence on Hand

- GitHub Issue [#17：实现初版 PPT 人工策展平台](https://github.com/dulltackle/PPTExtract/issues/17) 是实现阶段的完整规格来源，综合了 Issue #1 及其全部已完成决策票；发现真正矛盾时才重新请求决策。
- `CONTEXT.md` 与 `docs/adr/` 保存已定型的领域语言和架构决策。
- `docs/research/` 与 `fixtures/` 保存 anydoc、重复页脚噪声和代表性本地样本的调研证据。真实样本覆盖图表型页面、演讲者备注和长文档，不得上传、公开或成为公开 CI 的隐式依赖。
- 基线调研已确认 anydoc v0.1.8 的整文档输出没有页节点，图片也没有页归属；项目侧单页投影和转换适配因而不是可选优化。
- LibreOffice/Poppler 在 5 份本地样本共 200 页上都成功渲染；一份 43 页样本的两次 PDF 字节不同而逐页 PNG 一致。该证据支持初版路线，不构成与 PowerPoint 视觉等价的承诺。
- 5 页真实策展走查的中位数为 53 秒、范围 14–186 秒；该数据只用于发现图片处置和截图交互长尾，不外推产能或 SLA。
- `review-workbench-prototype.html`、`docs/review-workbench-interaction-spec.md` 与现有人工确认截图是交互先例，但静态原型不是可复用的产品实现，其早期 VLM 演示控件不得进入初版。
- 尚无正式自动化测试套件或公开可再分发的真实页素材。主验收缝应从上传 `.pptx` 到下载并验证完整 ZIP，把部署后的产品作为黑盒测试。

## Product Principles

- 人的明确判断优先于模型推断；审核时始终让来源证据、当前编辑值和继承来源可对照。
- 身份与内容严格分离；未变化内容不重复劳动，变化内容不静默继承。
- 高频路径键盘优先，硬校验阻止不完整结论，质量提示不打断保存。
- 只发布完整、不可变、可校验的快照；任何失败都不得移动当前产物指针。
- 真实语料默认保密，原型与公开说明不得泄露内容。

## Accessibility & Inclusion

键盘必须覆盖逐页策展的完整高频路径，焦点、审核状态与阻塞原因不能只靠颜色表达。具体合规等级尚未决定。
