# PPTExtract

把 `.pptx` 演示文稿变成可供下游 RAG 系统使用的知识单元。系统本身不判断什么是关键信息——它负责把每一页拆解、渲染、呈现给人，由人来判断并定型。

## Language

### 内容单位

**文档**：
一份被摄取的 `.pptx` 演示文稿，由系统分配身份并跨多次上传保持稳定，是版本的载体。文件名与内容哈希都不构成文档身份。
_Avoid_: 文件、PPT、演示文稿

**版本**：
同一文档的一次不可变内容记录。重新上传或回滚都会产生新版本；旧版本只读保留，用于追溯。
_Avoid_: 修订、revision

**当前版本**：
一个文档当前用于策展和产物的 ready 版本。新的 ready 版本生效后，原当前版本成为只读历史版本。
_Avoid_: 最新上传、活动版本

**作废版本**：
因所属文档错误等摄取错误而被认定从未有效的版本。它保留用于追溯，但不进入策展或产物；作废不等于回滚。
_Avoid_: 删除版本、回滚版本

**页**：
文档内跨版本延续的逻辑内容单位。页码只是页在当前版本中的位置；寻址、审核、幂等判定和导出都以页为单位。
_Avoid_: 幻灯片、slide、张

**页版本**：
一页在某一版本中的不可变内容记录，包含该次上传中的页码、页指纹与来源内容。
_Avoid_: 页快照、page revision

**页指纹**：
由页的文本与页内图片字节共同计算出的哈希，用于判断某一页在新版本中是否发生了变化。它不是页身份；不同的重复页可以具有相同页指纹。
_Avoid_: 页哈希、校验和

**页对应关系**：
两个版本中的页版本是否属于同一页的判定。存在多种合理对应时必须由人确认，新版本在确认前不能成为当前版本。
_Avoid_: 页匹配结果、自动匹配

**Chunk**：
导出给下游 RAG 的入库单元。当前阶段一页对应一个 chunk。
_Avoid_: 片段、分块、切片

**Chunk 正文**：
每个 Chunk 中供下游 RAG 直接使用的自足文本，由页的来源内容与经人工确认的标注确定性合成。
_Avoid_: 检索文本、拼接文本

**Chunk 内容哈希**：
由 Chunk 正文及其结构化内容计算出的哈希，用于识别产物内容变化；它不表示 Chunk 身份，也不同于页指纹。
_Avoid_: 页指纹、Chunk ID

### 人的活动

**策展**：
人工判断一页中哪些内容是关键信息，并将其定型的过程。本系统的核心活动。
_Avoid_: 抽取、提取、extraction

**发布**：
由人明确确认，把某一时点的可交付内容定型为新的当前产物。业务变化本身不会自动发布。
_Avoid_: 自动同步、实时导出

**转换**：
由 anydoc 完成的、从 `.pptx` 到结构化文本的机械格式变换。不含任何判断。
_Avoid_: 解析、抽取

**标注**：
人工为一页补充并确认的语义说明，以自足的自然语言结论为主，同时保留少量结构化信息；一页可包含页级总述和多个视觉对象子项。
_Avoid_: 注释、备注（后者专指演讲者备注）

**标注草稿**：
等待人工确认的候选标注；可由 VLM 生成，只有经过明确的逐页确认后才成为标注，确认时不要求必须修改原文。
_Avoid_: 自动标注、VLM 标注

**视觉对象**：
页内可由人逐一决定是否纳入策展的可视内容单元，例如图表、图片、地图、信息图或组合图形；它可由多个源元素组成，其范围可由人调整。
_Avoid_: 图片、配图、figure

**视觉对象引用**：
视觉对象的全局唯一、不透明身份，在页未变化时保持稳定；页发生变化后不沿用，对外字段名为 `visual_ref`。
_Avoid_: 图片 ID、资产 ID、figure ref

**视觉资产**：
视觉对象被纳入策展后，从标准页渲染结果中裁出的 PNG 图像；它随产物交付并由对应的标注子项引用。
_Avoid_: 原图、截图、图片文件

**审核状态**：
一个已启用策展的页版本的人工策展结论，取值为 pending、approved 或 excluded。只有 approved 的当前页版本进入产物；版本是否当前与软删生命周期不属于审核状态。
_Avoid_: 状态、flag

### 产出

**产物**：
交付给下游的完整快照，由 approved、未软删页的 JSONL Chunk 与这些 Chunk 引用的视觉资产组成。
_Avoid_: 输出、结果、导出物

**发布候选**：
一次待人工确认的发布所绑定的候选业务状态及其相对当前产物的变更摘要；确认前业务状态变化会使其失效。
_Avoid_: 候选修订、publication revision

**当前产物**：
最近一次成功发布、下游应整体采用的产物。它不是当前业务状态的实时视图，新发布成功前仍保持上一份。
_Avoid_: 最新包、实时快照

**软删**：
文档或页被移除时打上墓碑标记而非物理删除，使下游能够感知失效。
_Avoid_: 删除、归档

### 来自源文件的元素

**演讲者备注**：
`.pptx` 中附于每页的备注文本。往往是该页信息密度最高的部分，纳入正文。
_Avoid_: 注释、标注

**隐藏页**：
在演示中被跳过的页。它默认只登记在版本中，不转换且没有审核状态；人工启用后才进入策展。
_Avoid_: 跳过页
