# PPTExtract

把 `.pptx` 演示文稿变成可供下游 RAG 系统使用的知识单元。系统本身不判断什么是关键信息——它负责把每一页拆解、渲染、呈现给人，由人来判断并定型。

## Language

### 内容单位

**文档**：
一份被摄取的 `.pptx` 演示文稿。跨多次上传保持同一身份，是版本的载体。
_Avoid_: 文件、PPT、演示文稿

**版本**：
同一文档的一次内容变更。重新上传产生新版本，旧版本保留可追溯。
_Avoid_: 修订、revision

**页**：
演示文稿中的一张幻灯片。系统的原子单位——寻址、审核、幂等判定、导出都以页为单位。
_Avoid_: 幻灯片、slide、张

**页指纹**：
由页的文本与页内图片字节共同计算出的哈希，用于判断某一页在新版本中是否发生了变化。
_Avoid_: 页哈希、校验和

**Chunk**：
导出给下游 RAG 的入库单元。当前阶段一页对应一个 chunk。
_Avoid_: 片段、分块、切片

### 人的活动

**策展**：
人工判断一页中哪些内容是关键信息，并将其定型的过程。本系统的核心活动。
_Avoid_: 抽取、提取、extraction

**转换**：
由 anydoc 完成的、从 `.pptx` 到结构化文本的机械格式变换。不含任何判断。
_Avoid_: 解析、抽取

**标注**：
人工为一页补充的信息，主要是对图表内容的文字描述。VLM 可以生成草稿，但未经人工确认的草稿不是标注。
_Avoid_: 注释、备注（后者专指演讲者备注）

**审核状态**：
一页在策展流程中所处的位置。只有 approved 的页会进入产物。
_Avoid_: 状态、flag

### 产出

**产物**：
交付给下游的导出结果，只包含 approved 的页。
_Avoid_: 输出、结果、导出物

**软删**：
文档或页被移除时打上墓碑标记而非物理删除，使下游能够感知失效。
_Avoid_: 删除、归档

### 来自源文件的元素

**演讲者备注**：
`.pptx` 中附于每页的备注文本。往往是该页信息密度最高的部分，纳入正文。
_Avoid_: 注释、标注

**隐藏页**：
在演示中被跳过的页。默认不进入策展流程，可手动启用。
_Avoid_: 跳过页
