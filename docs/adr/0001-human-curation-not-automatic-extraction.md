# 关键信息由人工策展决定，而非自动抽取

语料的领域不限且持续扩张，其中包含大量图表型页面——信息存在于图里，文字只有一句标题——这使得基于版式和字号的规则式抽取没有立足点，而纯 LLM 抽取的产出质量无人验证。我们决定让系统只做机械转换与呈现，由人在审核界面中逐页判断哪些是关键信息；VLM 只能生成待确认的草稿，不能直接产生标注。

## Consequences

人工工时成为系统吞吐的上限，因此审核界面的效率就是产品能力本身：页的免重审继承（见 ADR-0002）与标注输入的交互设计不是优化项，而是核心需求。同时我们没有自动化的质量评测，审核环节是唯一的质量闸门。
